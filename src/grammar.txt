expr    ::= keyword:var identifier = expr;
        ::= term (('+' | '-') term)*

term    ::= factor (('*' | '/') factor)*

factor  ::= INT/LONGINT/FLOAT/DOUBLE
        ::= ('+' | '-') factor
        ::= ( expr )